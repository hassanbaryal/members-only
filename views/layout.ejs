<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/layout.css">
  <link rel="stylesheet" href="/stylesheets/homepage.css">
  <link rel="stylesheet" href="/stylesheets/member.css">
  <link rel="stylesheet" href="/stylesheets/postPage.css">
  <link rel="stylesheet" href="/stylesheets/profile.css">
  <title><%= title %></title>
</head>
<body>
  <header>
    <div class="banner">
      <% if (locals.currentUser.member) { %>
        <img src="/images/unlockedLock.svg" alt="Unlocked Lock">
      <% } else { %>
        <img src="/images/lockedLock.svg" alt="Unlocked Lock">
      <% } %>
      <span>Members Only</span>
    </div>
    <div class="nav-links">
      <% if (!locals.currentUser.member) { %>
        <a href="/member" style="font-weight: bold;">Become a Member!</a>  
      <% } %>
      <a href="/homepage">Home Page</a>
      <% if (locals.currentUser.member) { %>
        <a href="/profile/">Profile</a>
      <% } else { %>
        <a href="/member">Profile</a>
      <% } %>
      <a href="/logout" style="font-weight: bold;"> Log Out</a>
      <!-- <p><%= locals.currentUser.username %></p> -->
      </div>
  </header>
